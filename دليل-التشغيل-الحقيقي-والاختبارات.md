# دليل التشغيل الحقيقي ونتائج الاختبارات

**التاريخ:** 24 يوليو 2026
**الحالة:** جاهز للتجهيز على Staging، وليس إعلاناً بأن خدمة الإنتاج الخارجية تعمل بالفعل.

## ما تم تنفيذه

- إزالة ملف `backend/.env` من المستودع لأنه ملف أسرار محلي.
- إضافة قواعد منع تتبع ملفات البيئة إلى `.gitignore`.
- استبدال بيانات الأسواق الثابتة في الخادم بمسار يعتمد على مزود بيانات خارجي مضبوط من البيئة.
- عند عدم ضبط مزود الأسواق، يرجع الخادم حالة واضحة `503` بدلاً من عرض أسعار مختلقة.
- إضافة متغيري `MARKET_DATA_URL` و`MARKET_DATA_API_KEY` إلى `backend/.env.example`.
- تشغيل فحوصات الواجهة والخادم محلياً بعد تثبيت الاعتماديات. ملاحظة: إضافة Workflow إلى GitHub تحتاج صلاحية `workflows` غير متاحة لحساب الاتصال الحالي، لذلك لم يتم دفع ملف Workflow.
- إبقاء كل الأسرار خارج Git وعدم وضعها في متغيرات `VITE_*`.

## نتائج الاختبار المحلي

تم تثبيت الاعتماديات من ملفات lock ثم تشغيل الفحوصات:

| الفحص | النتيجة |
|---|---|
| بناء الواجهة `npm run build` | ناجح |
| اختبارات الواجهة `npm test -- --run` | ناجح: 4 اختبارات |
| ESLint | ناجح: 0 أخطاء، 7 تحذيرات React Hooks |
| بناء الخادم `backend/npm run build` | ناجح |
| npm audit للواجهة | 0 ثغرات معروفة |
| npm audit للخادم | 0 ثغرات معروفة |

تحذيرات ESLint الحالية غير مانعة للبناء، لكنها تحتاج تنظيفاً قبل سياسة جودة صارمة: ثلاثة في `AdminLayout`، وواحد في `SubAdmins`، وواحد في `MarketsManager`، وواحد في `ServicesManager`، وواحد في `dashboard`.

## إعداد مزود بيانات الأسواق

يجب توفير endpoint حقيقي يعيد JSON بأحد الشكلين:

- مصفوفة مباشرة من الأسعار.
- كائن يحتوي على `data` وهي مصفوفة الأسعار.

كل عنصر يجب أن يحتوي على الأقل على:

- `symbol`
- `price` أو `close` أو `last`
- `change` أو `changeValue`

ويمكن أن يحتوي على `name` و`nameAr` و`currency` و`changePercent`.

اضبط على الخادم:

- `MARKET_DATA_URL`
- `MARKET_DATA_API_KEY` إذا كان المزود يحتاج مفتاحاً

لا يمكن اعتبار بيانات الأسواق حقيقية قبل وضع endpoint مرخص ومفتاحه في بيئة الخادم، ثم اختبار `/api/markets/ticker` عبر HTTPS.

## خطوات النشر الفعلية

1. إنشاء ملف البيئة على الخادم فقط من `backend/.env.example`.
2. ضبط `NODE_ENV=production`.
3. ضبط `DATABASE_URL` لقاعدة PostgreSQL production.
4. توليد `JWT_SECRET` و`JWT_REFRESH_SECRET` عشوائيين ومختلفين.
5. توليد hash لكلمة مرور Super Admin قوية وفريدة.
6. ضبط `ALLOWED_ORIGINS` على نطاق الواجهة الحقيقي فقط.
7. ضبط `APP_URL` و`API_URL` و`SOCKET_URL` الحقيقية.
8. ضبط `MARKET_DATA_URL` لمزود بيانات حقيقي أو إبقاء ميزة الأسواق غير مفعلة بوضوح.
9. تشغيل `npm ci` داخل `backend`.
10. تشغيل `npx prisma generate`.
11. تطبيق `npx prisma migrate deploy` على Staging أولاً ثم Production.
12. تشغيل `npm run build` ثم PM2.
13. إعداد Nginx وTLS وWebSocket.
14. تنفيذ health check من خارج الخادم.
15. نشر الواجهة مع `VITE_API_URL` و`VITE_SOCKET_URL` الحقيقيين.

## اختبار القبول قبل فتح الخدمة

- دخول Super Admin وتسجيل الخروج.
- تغيير كلمة المرور ثم التأكد من إبطال الجلسات القديمة.
- دخول العميل والتأكد من أنه لا يرى أي عميل آخر.
- اختبار كل صلاحية للمشرف الفرعي قراءة وكتابة.
- إنشاء عميل ومحفظة ومعاملة وتذكرة واجتماع.
- تحديث المحتوى من لوحة الإدارة والتأكد من ظهوره للعامة.
- اختبار تحديث Socket.io خلف Nginx وTLS.
- اختبار فشل مزود الأسواق وعدم عرض أرقام ثابتة.
- اختبار النسخ الاحتياطي والاستعادة.
- اختبار الهاتف واللغة العربية والإنجليزية.

## حدود ما تم إنجازه في هذه الدفعة

لا يمكن من داخل المستودع إنشاء DNS أو VPS أو PostgreSQL أو شهادة TLS أو حساب مزود الأسواق أو مفاتيح دفع/بريد حقيقية. لذلك أصبح الكود **حقيقياً من ناحية التكامل وعدم اختلاق بيانات الأسواق**، لكن تشغيل الإنتاج الخارجي ما زال يحتاج بيانات البنية التحتية والحسابات التي يملكها صاحب المشروع.

لا توجد مدفوعات أو KYC أو بريد/SMS فعلي في نطاق الكود الحالي. إذا كانت مطلوبة للمنتج، يجب ربطها بمزود معتمد واختبارها قبل الإطلاق.
