# سياسة الأمان — ثروة كابيتال

## الإبلاغ عن ثغرة

لا تُنشر الثغرات في Issues عامة. يجب إرسال الوصف والأثر وخطوات إعادة الإنتاج إلى قناة الأمان الداخلية المعتمدة لدى مالك المنصة.

## حدود الثقة

- المتصفح لا يتصل بقاعدة البيانات مباشرة.
- Express/Prisma هما بوابة البيانات الوحيدة.
- بيانات العمل تحفظ في PostgreSQL.
- صلاحيات الإدارة تفرض في الخادم، وليس في عناصر الواجهة فقط.
- غرف Socket.io تُحدد من JWT الموثق.

## المصادقة والجلسات

- كلمات المرور تُحفظ بواسطة bcrypt.
- Access Token قصير العمر.
- Refresh Tokens تحفظ خادمياً كبصمات SHA-256 وتُدوّر عند الاستخدام.
- يمكن إبطال جلسات المستخدم عند تعليق الحساب أو تغيير كلمة المرور.
- تسجيل الخروج يبطل Refresh Token ويمسح بيانات الجلسة من المتصفح.
- محاولات الدخول تسجل في PostgreSQL وتخضع لحدود طلبات مستقلة.

## التفويض

- العميل لا يرى إلا موارده.
- المشرف الفرعي يخضع للصلاحيات المخزنة في حسابه.
- العمليات الحساسة تتطلب دوراً أو صلاحية خادمية مناسبة.
- استجابات المستخدمين لا تتضمن password hashes.

## حماية البنية

- TLS على Vercel وNginx.
- CORS بقائمة مصادر صريحة.
- Helmet وترويسات أمان.
- Rate limiting في Nginx وExpress.
- PostgreSQL غير مكشوف للإنترنت العام.
- التطبيق يعمل بمستخدم خدمة محدود على VPS.

## متطلبات ما قبل الإنتاج

- أسرار JWT عشوائية وقوية ومختلفة.
- بريد وكلمة مرور Super Admin مضبوطتان من بيئة الخادم.
- `ALLOWED_ORIGINS` يحتوي نطاق الواجهة الحقيقي فقط.
- لا توجد حسابات أو كلمات مرور تجريبية.
- migrations مطبقة ومختبرة على staging.
- النسخ الاحتياطية والاستعادة مختبرتان.
- سجل الأخطاء ومراقبة الموارد مفعّلان.
- كل حسابات الإدارة تستخدم كلمات مرور فريدة.

## البيانات الحساسة

- لا تُكتب كلمات المرور أو التوكنات أو بيانات الاتصال بقاعدة البيانات في السجلات.
- لا تُحفظ أسرار خادمية في متغيرات `VITE_*`.
- بيانات العملاء لا تُرسل إلا للواجهات المخولة.
- حذف العميل من الواجهة يعني الأرشفة للحفاظ على السجل الاستشاري.
