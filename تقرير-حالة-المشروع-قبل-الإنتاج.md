# وثيقة مراجعة شاملة: حالة ثروة كابيتال قبل الإنتاج

**تاريخ المراجعة:** 24 يوليو 2026
**الفرع:** `arena/019f94a1-tharawa-studio`
**نطاق المراجعة:** جميع الملفات الموجودة في المستودع، دون استثناء
**التغيير البرمجي أثناء المراجعة:** لم تتم كتابة أو تعديل أي كود. تمت إضافة هذه الوثيقة فقط.

## 1. خلاصة تنفيذية

المشروع هو منصة استشارات واستثمار عربية/إنجليزية، تتكون من واجهة React/Vite مستضافة نظرياً على Vercel، وخادم Express/Node.js مع Prisma وPostgreSQL وSocket.io على VPS. توجد لوحة إدارة واسعة، بوابة عميل، صفحات عامة، ونظام CMS.

**التقدير الحالي:** المشروع في مرحلة **Backend-First / Staging Candidate**، وليس جاهزاً لاستقبال مستخدمين حقيقيين بعد.

السبب الرئيسي ليس نقص الواجهة؛ الواجهة والـ API والـ schema موجودة بدرجة جيدة. العوائق الحاسمة هي:

1. لا توجد بيئة إنتاج حقيقية مضبوطة: النطاقات والأسرار وقاعدة البيانات وTLS وVPS ما زالت أمثلة.
2. ملف `backend/.env` متتبع داخل Git ويحتوي قيماً placeholder مع `NODE_ENV=production`، وهذا خطر تشغيلي وأمني.
3. بيانات الأسواق ما زالت Mock صراحة، وليست بيانات حية.
4. لا يوجد مزود بريد/SMS/Push أو مدفوعات أو تحقق هوية؛ تدفقات هذه الخدمات غير إنتاجية.
5. لم يمكن تشغيل build أو lint أو tests في بيئة المراجعة لأن الاعتماديات غير مثبتة (`tsc`, `eslint`, `vitest` غير موجودة).
6. توجد فجوة توثيق/تنفيذ في التحديث اللحظي: الخادم يرسل أحداث Socket.io، والعميل يحولها إلى أحداث متصفح، لكن بعض طبقات الاستعلام لا تستمع مباشرة إلا عبر هذا المسار المركب؛ يلزم اختبار فعلي لكل شاشة.
7. توكنات الدخول والتجديد محفوظة في `localStorage`، ما يزيد أثر أي XSS ناجح مقارنة بملفات HttpOnly cookies.

**الخلاصة:** يمكن اعتبار المشروع أساساً قوياً لبيئة staging، لكن لا ينبغي إعلان الإنتاج قبل إغلاق البنود الحرجة وإثباتها باختبارات ونشر فعلي.

## 2. ما تمت قراءته ومراجعته

تمت مراجعة الملفات النصية في:

- ملفات المشروع الجذرية وملفات إعداد Vite/TypeScript/ESLint وVercel.
- جميع ملفات `src`، بما فيها الصفحات العامة، لوحة الإدارة، بوابة العميل، المكونات، السياقات، طبقات API وCMS والأمن والمصادقة والتخزين والاختبارات.
- جميع ملفات `backend`، بما فيها الخادم، كل مسارات API، middleware المصادقة، Prisma schema، migration، seed، إعدادات البيئة، Nginx، PM2، وسكربت إعداد VPS.
- جميع وثائق README وDeployment وDNS وSecurity وتقارير التكامل والتدقيق والجاهزية.
- ملفات `public` وmanifest وrobots وsitemap وملفات الصور كملفات ثنائية من حيث وجودها وأسمائها وأحجامها.
- ملفات lock الخاصة بالواجهة والخادم.

الملفات الثنائية (الأيقونات والصور) لا تحتوي نصاً قابلاً للمراجعة البرمجية؛ تم التحقق من وجودها ومساراتها وأحجامها.

## 3. ما هو جاهز أو قريب من الجاهزية

### 3.1 الواجهة العامة — جاهزة وظيفياً بدرجة جيدة

- صفحات الصفحة الرئيسية، عن الشركة، الخدمات، تفاصيل الخدمة، الأسواق، الأخبار، المقال، الأسئلة الشائعة، الاتصال، وتسجيل الدخول موجودة.
- دعم العربية والإنجليزية واتجاه RTL/LTR موجود عبر `LanguageContext`.
- المكونات العامة المشتركة موجودة: Header وFooter وCookie Banner وWhatsApp وBack to Top وAnnouncement Bar.
- توجد معالجة أخطاء عامة عبر Error Boundary ومعالجات أخطاء المتصفح.
- توجد تهيئة SEO أساسية في `index.html`، وmanifest، وrobots، وsitemap.
- توجد سياسة CSP وترويسات أمنية في `vercel.json`، مع الحاجة لمراجعة النطاقات الفعلية قبل النشر.

### 3.2 لوحة الإدارة — موجودة وبنطاق واسع

- تسجيل دخول أدمن، جلسات، أدوار `super/admin/sub`، وصلاحيات للمشرف الفرعي.
- شاشات العملاء، الملفات، المحافظ، المعاملات، الرسائل، الإشعارات، الاجتماعات، التقارير، المهام، البحث، الفريق، الإعدادات، الأمان، والمحتوى.
- CMS لأقسام Hero والخدمات والأسواق والأسئلة والشهادات وعن الشركة والتصميم والخصوصية.
- توجد طبقة توافق تنقل بعض عقود الواجهة القديمة إلى API الخلفي.
- الحذف في مسارات العملاء مصمم كأرشفة بدلاً من حذف السجل الاستشاري، وهي نقطة مناسبة لطبيعة النظام.

### 3.3 الخادم وقاعدة البيانات — أساس معماري جيد

- Express وHelmet وCORS وrate limiting وmorgan موجودة.
- PostgreSQL وPrisma schema وmigration إنتاجية موجودة.
- نماذج المستخدمين والمحافظ والأصول والمعاملات والتذاكر والردود والإشعارات والاجتماعات والمحتوى والإعدادات والجلسات وسجل التدقيق موجودة.
- access JWT قصير نسبياً في الإعدادات النموذجية، وrefresh tokens تحفظ كبصمات SHA-256 مع تدوير وإبطال.
- تقييد الأدوار والصلاحيات موجود على الخادم، وليس على الواجهة فقط.
- مسارات العميل تقيد الوصول بمالك المورد في عدة مواضع، ومسارات الإدارة محمية.
- يوجد health check مع اختبار اتصال قاعدة البيانات.
- Socket.io موثق عبر JWT، والغرف مشتقة من هوية التوكن بدلاً من هوية يرسلها المتصفح.
- توجد ملفات نشر لـ Nginx وPM2 وسكربت إعداد VPS ووثائق تشغيل.

### 3.4 البيانات والتكامل

- API client مركزي مع timeout وتجديد التوكن ومعالجة أخطاء.
- TanStack Query مستخدم للاستعلامات وإبطال cache.
- CMS العام يقرأ من API مع fallback للعرض.
- نموذج الاتصال ومسارات العملاء والرسائل والمعاملات مرتبطة مبدئياً بالخادم.
- البيانات التجريبية مقيدة في seed بشرط ألا تكون البيئة production، وهي ممارسة صحيحة من حيث المبدأ.

## 4. المشاكل والعوائق التي تمنع الإنتاج

### أ. عوائق حرجة — يجب إغلاقها قبل أي إطلاق

#### أ.1 ملف بيئة حساس متتبع في Git

`backend/.env` موجود ضمن الملفات المتتبعة، مع أنه يفترض أن يكون ملفاً محلياً خارج Git. الملف الحالي ليس مضبوطاً فعلياً؛ يحتوي `DATABASE_URL` تجريبياً، وJWT placeholders، ونطاقات وهمية، وبيانات admin غير مكتملة. وجود هذا الملف متتبعاً يخلق احتمال تسريب أسرار مستقبلية ويخالف تعليمات README وSECURITY.

**الحالة:** غير جاهز.
**المطلوب:** إزالته من التتبع، إضافة قاعدة صريحة في ignore، تدوير أي أسرار سبق استخدامها، وإنشاء ملف إنتاج خارج المستودع فقط.

#### أ.2 لا يوجد نطاق إنتاج حقيقي مضبوط

الوثائق وCSP وNginx ومتغيرات البيئة تستخدم `yourdomain.com` و`your-domain.com`. لا يوجد دليل من المستودع على DNS فعلي أو Vercel production domain أو API domain حقيقي.

**الحالة:** غير جاهز.
**المطلوب:** ضبط DNS، Vercel، API domain، CORS، CSP، Socket URL، TLS، ثم اختبارها من متصفح حقيقي.

#### أ.3 أسرار الإنتاج وSuper Admin غير منشأة

`JWT_SECRET` و`JWT_REFRESH_SECRET` و`SUPER_ADMIN_PASSWORD_HASH` ما زالت placeholders في الملف المتتبع، ولا يوجد إثبات لإنشاء حساب إداري إنتاجي بكلمة مرور فريدة قوية.

**الحالة:** غير جاهز.
**المطلوب:** توليد أسرار عشوائية مختلفة، حفظها في secret manager أو بيئة VPS، إنشاء admin بلا كلمة مرور تجريبية، وتجربة الدخول وتغيير كلمة المرور وإبطال الجلسات.

#### أ.4 فحوصات الجودة لم تُثبت

محاولات التشغيل في هذه المراجعة:

- `npm run build` للواجهة: تعذر التشغيل لأن `tsc` غير مثبت.
- `npm test`: تعذر التشغيل لأن `vitest` غير مثبت.
- `npm run lint`: تعذر التشغيل لأن `eslint` غير مثبت.
- `backend npm run build`: تعذر التشغيل لأن `tsc` غير مثبت.

هذا لا يثبت وجود أخطاء في الكود، لكنه يعني عدم وجود دليل قابل للتسليم على أن المشروع يبني ويجتاز الاختبارات في بيئة نظيفة.

**الحالة:** غير مثبتة.
**المطلوب:** تثبيت الاعتماديات، تشغيل build للواجهة والخادم، Prisma generate، migration على staging، lint، test، ثم حفظ نتائج CI.

#### أ.5 الأسواق Mock وليست إنتاجية

مسار `/api/markets/ticker` يعيد بيانات ثابتة، ويصف المصدر صراحة بأنه Mock. هذا غير مقبول إذا كانت الواجهة تسميها أسعاراً لحظية أو تعرضها كمعلومات سوق فعلية.

**الحالة:** غير جاهز.
**المطلوب:** إما ربط مزود بيانات مرخص مع caching وtimeout وfallback واضح، أو تغيير الواجهة والنصوص إلى بيانات توضيحية غير استثمارية قبل الإطلاق.

#### أ.6 الخدمات الخارجية غير موجودة

لا يوجد تكامل فعلي للبريد أو SMS أو Push أو الدفع والتحويلات أو KYC. وثيقة النشر تصفها صراحة بأنها Mock/مؤجلة.

**الحالة:** تعتمد على نطاق المنتج.
**المطلوب:** إذا كان الإطلاق يتطلب هذه الخدمات، فهي blockers. وإذا كان المنتج استشارياً يدوياً فقط، يجب إزالة أي إيحاء بأن التنفيذ أو الدفع أو التداول يحدث آلياً، وإظهار حالات الطلب اليدوية بوضوح.

#### أ.7 تخزين access وrefresh tokens في localStorage

المصادقة الأمامية تحفظ التوكنات في `localStorage`. هذا يجعل سرقة توكنات الجلسة ممكنة عند نجاح XSS أو إضافة طرف ثالث غير موثوقة. توجد sanitization وCSP، لكنها لا تلغي الخطر.

**الحالة:** مخاطرة عالية قبل منصة مالية.
**المطلوب:** تقييم الانتقال إلى refresh token في cookie من نوع HttpOnly/Secure/SameSite، تقليل مدة access token، وإجراء اختبار XSS ومراجعة CSP وموارد الطرف الثالث.

### ب. مشاكل عالية الأهمية

#### ب.1 تناقضات بين وثائق البيئة والإعدادات

- ملف `.env.example` يذكر JWT access لمدة 15 دقيقة، بينما `backend/.env` يذكر 8 ساعات.
- الوثائق تستخدم نطاقات باسمين مختلفين: `yourdomain.com` و`your-domain.com`.
- DNS يذكر `APP_DOMAIN` و`API_DOMAIN`، لكن الخادم لا يعتمد عليهما مباشرة في إعداداته.
- README يشير إلى `.github/workflows/ci.yml`، لكن لا يوجد هذا الملف في checkout الذي تمت مراجعته.

**الأثر:** نشر خاطئ أو إحساس زائف بوجود CI.

#### ب.2 seed يحتاج بوابة تشغيل أوضح

الـ demo data متوقف في production بشرط صحيح، لكن تشغيل seed في production ينشئ Super Admin وبيانات content الافتراضية. يجب تحديد هل seed جزء من provisioning الأول فقط أم من كل deploy، ومنع إعادة كتابة محتوى يديره الموظف، وتوثيق ذلك بوضوح.

#### ب.3 صلاحيات ومسارات تحتاج اختبار مصفوفة شامل

توجد حماية جيدة على مستوى الخادم، لكن يلزم اختبار كل مسار لكل دور: عميل، مشرف فرعي بصلاحية قراءة، مشرف فرعي بصلاحية كتابة، admin، super. لا يكفي إخفاء عناصر القائمة في الواجهة.

#### ب.4 التحديث اللحظي يحتاج اختبار قبول فعلي

الخادم يرسل `admin_update` و`client_update` و`content_updated` و`settings_updated`، والعميل يحولها إلى أحداث داخلية. يجب إثبات أن كل شاشة تعيد تحميل البيانات الصحيحة، وأن إعادة الاتصال وتجديد التوكن لا ينتجان غرفة قديمة أو تحديثاً لمستخدم آخر.

#### ب.5 التحقق من المدخلات غير موحد بالكامل

يوجد Zod في بعض مسارات الخادم والواجهة، لكن عدداً من مسارات الإدارة يعتمد على قراءة `req.body` مباشرة مع تحقق محدود. يلزم توحيد schema للـ create/update للعميل والمحفظة والرسائل والمعاملات والاجتماعات والمشرفين، مع حدود للأرقام والنصوص والحالات المسموح بها.

#### ب.6 سجلات ومراقبة الإنتاج غير مكتملة

توجد morgan وaudit وبعض logger، لكن لا يوجد دليل على Sentry/monitoring فعلي أو تنبيهات أو سياسة احتفاظ للسجلات أو إخفاء شامل للبيانات الحساسة في سجلات الطلبات. يلزم التأكد خصوصاً من عدم تسجيل Authorization headers أو بيانات العملاء.

#### ب.7 النسخ الاحتياطي والاستعادة غير مثبتين

يوجد أمر `pg_dump` في الوثيقة، لكن لا يوجد دليل على جدولة النسخ أو نسخ خارج VPS أو اختبار restore أو تحديد RPO/RTO.

#### ب.8 الخصوصية والامتثال التجاري

النظام يتعامل مع بيانات عملاء واستثمار. توجد صفحة سياسة خصوصية قابلة للإدارة، لكن لا يوجد في المستودع إثبات لمراجعة قانونية، أو سياسة احتفاظ وحذف، أو موافقات، أو تصنيف بيانات، أو خطة استجابة لحوادث. يجب اعتمادها من مالك المنتج والمستشار القانوني قبل الإطلاق.

## 5. ما هو موجود لكنه ليس دليلاً على الجاهزية

- وجود ملفات `DEPLOYMENT.md` و`PRODUCTION_READY_REPORT.md` لا يعني أن VPS وVercel وTLS وDNS شُغلت فعلياً.
- وجود migration لا يعني أنها طُبقت على قاعدة production ويمكن استعادتها.
- وجود test file واحد (`src/lib/security.test.ts`) لا يعني تغطية النظام.
- وجود Socket.io client/server لا يعني أن realtime يعمل خلف Nginx وTLS.
- وجود fallback وseed لا يعني أن بيانات الإنتاج صحيحة أو أن fallback آمن.
- نجاح build، بعد تثبيت الاعتماديات، لا يغطي الاختبارات الوظيفية والأمنية والتشغيلية.

## 6. قائمة ما قبل الإنتاج المطلوبة

### بيئة وبنية

- [ ] اختيار الدومين الحقيقي وتثبيت قيمه في Vercel وVPS وCSP وNginx.
- [ ] حذف `backend/.env` من Git وتدوير الأسرار إن لزم.
- [ ] إنشاء PostgreSQL production مع مستخدم محدود وصلاحيات أقل ما يمكن.
- [ ] ضبط firewall وSSH بمفتاح، وعدم تشغيل التطبيق بصلاحيات root.
- [ ] إصدار TLS وتجربة التجديد التلقائي.
- [ ] تشغيل PM2 مع restart بعد reboot وسجلات محمية.
- [ ] تطبيق migration على staging ثم production.
- [ ] إنشاء نسخ احتياطية مشفرة خارج VPS وتجربة restore موثقة.

### جودة وبرمجيات

- [ ] `npm ci` للواجهة والخادم في بيئة نظيفة.
- [ ] `npm run build` للواجهة.
- [ ] `npm run build` للخادم.
- [ ] `prisma generate` و`prisma migrate deploy`.
- [ ] `npm run lint` و`npm test` وقياس coverage.
- [ ] إضافة/تفعيل CI فعلي، وتصحيح README إذا لم يكن CI موجوداً.
- [ ] فحص npm audit ومراجعة الاعتماديات وتثبيت lockfiles.

### أمان

- [ ] أسرار JWT عشوائية مختلفة وطويلة.
- [ ] Super Admin إنتاجي بلا بيانات تجريبية.
- [ ] اختبار login lockout وrefresh rotation وlogout وchange-password.
- [ ] اختبار عزل العميل عن بيانات عميل آخر.
- [ ] اختبار صلاحيات كل دور وكل endpoint.
- [ ] مراجعة localStorage tokens واتخاذ قرار موثق أو نقلها إلى HttpOnly cookies.
- [ ] اختبار XSS وCSRF وCORS وCSP وrate limiting.
- [ ] التأكد من عدم ظهور hashes أو tokens أو بيانات حساسة في الاستجابات والسجلات.

### وظائف المنتج

- [ ] اختبار نموذج الاتصال ووصول الرسالة للوحة الإدارة.
- [ ] اختبار إنشاء طلب إيداع/سحب وتحديثه وإظهاره للعميل.
- [ ] اختبار التذاكر والردود وحالات القراءة.
- [ ] اختبار إدارة المحتوى بالعربية والإنجليزية وظهوره للعامة.
- [ ] اختبار الاجتماعات والإشعارات والإعدادات.
- [ ] اتخاذ قرار نهائي بخصوص بيانات الأسواق، البريد، الدفع، KYC.
- [ ] إضافة تنبيه واضح بأن المنصة استشارية ولا تنفذ تداولاً أو دفعاً آلياً إذا كان ذلك هو نطاق المنتج.

### أداء وتجربة مستخدم

- [ ] اختبار الهاتف والكمبيوتر وRTL/LTR.
- [ ] فحص الصور والأيقونات وfavicon وmanifest وsitemap.
- [ ] فحص Lighthouse وCore Web Vitals.
- [ ] التأكد من أن CSP لا يمنع الخطوط أو API أو WebSocket.
- [ ] اختبار offline/network failure ورسائل الخطأ وعدم وجود شاشات فارغة.
- [ ] اختبار refresh المباشر لكل route على Vercel.

## 7. ترتيب التنفيذ المقترح

### المرحلة الأولى — الإغلاق الأمني والتشغيلي

إزالة ملف البيئة من Git، تدوير الأسرار، تجهيز النطاق وVPS وقاعدة البيانات وTLS والنسخ الاحتياطي، ثم إنشاء بيئة staging منفصلة.

### المرحلة الثانية — إثبات البناء والاختبارات

تثبيت الاعتماديات عبر lockfiles، تشغيل build/lint/test/Prisma، إضافة CI، وإغلاق أي أخطاء فعلية تظهر.

### المرحلة الثالثة — اختبار القبول والتكامل

اختبار المصادقة والصلاحيات وعزل البيانات وCMS والنماذج والمعاملات والتحديث اللحظي خلف Nginx، مع متصفح حقيقي وحسابات اختبار منفصلة.

### المرحلة الرابعة — قرار الخدمات الخارجية

تحديد ما إذا كان المنتج يظل استشارياً يدوياً. إذا نعم، تنظيف النصوص من ادعاءات التداول والأسعار اللحظية والدفع. إذا لا، ربط مزودي الأسواق والدفع والإشعارات وKYC مع اختبار فشل وتسويات وسجلات.

### المرحلة الخامسة — إطلاق محدود

إطلاق staging مع مستخدمين داخليين، ثم canary محدود، مراقبة الأخطاء والموارد، والتحقق من النسخ الاحتياطي والاستعادة، وبعدها فقط إطلاق عام.

## 8. الحكم النهائي

**ما هو جاهز:** بنية المشروع، معظم الواجهات، مخطط قاعدة البيانات، REST API الأساسي، المصادقة الخلفية، لوحة الإدارة، CMS، ملفات النشر، وأساس التحديث اللحظي.

**ما يتبقى:** تحويل الأساس إلى تشغيل مثبت: بيئة إنتاج حقيقية، أسرار ونطاقات صحيحة، حذف ملف البيئة المتتبع، اختبارات build/CI، اختبار صلاحيات وأمان، نسخ احتياطي واستعادة، اختبار realtime، وقرار/تنفيذ الخدمات الخارجية.

**قرار المراجعة:** لا أوصي بالإنتاج العام حالياً. أوصي بقبول المشروع كـ **مرشح staging بعد معالجة ملف البيئة والأسرار**، ثم إعادة تقييمه بعد نجاح قائمة التحقق كاملة.
